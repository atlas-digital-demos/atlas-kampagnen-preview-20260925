const menu=document.querySelector('.menu'),links=document.querySelector('.navlinks');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));links.classList.toggle('open',open);menu.textContent=open?'SCHLIESSEN ×':'MENÜ ☰'});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=document.querySelectorAll('[data-reveal]');if(reduced)reveal.forEach(e=>e.classList.add('shown'));else{const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('shown');io.unobserve(e.target)}}),{threshold:.1});reveal.forEach(e=>io.observe(e))}
addEventListener('scroll',()=>document.documentElement.style.setProperty('--progress',String(scrollY/Math.max(1,document.documentElement.scrollHeight-innerHeight))),{passive:true});
